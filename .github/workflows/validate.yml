name: Validate tactics

# 任何分支 push / PR 都校验全部战术脚本（与 viewer 播放器同一套规则）
on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build index (validate all tactic scripts)
        run: node tools/build-index.js
